# 복사해서 .env로 쓴다: cp .env.example .env
# .env는 커밋 대상이 아니다 (.gitignore)
DB_PASSWORD=여기에_비밀번호
SPRING_PROFILES_ACTIVE=mock
# 백엔드를 직접 호출할 때만 의미 있는 값. nginx 경유 접속에는 영향이 없다
CORS_ALLOWED_ORIGINS=http://15.164.215.132
